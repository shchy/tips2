machine:
    timezone:
        Asia/Tokyo
    services:
        - docker

dependencies:
    cache_directories:
        - "~/cache"
    pre:
        - docker build -t tips .

test:
    override:
        - docker run tips dnx -p test/TipsWeb.test/project.json test -parallel none
