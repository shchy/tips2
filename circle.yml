machine:
    timezone:
        Asia/Tokyo
    services:
        - docker

dependencies:
    cache_directories:
        - "~/cache"
    pre:
        - docker build -t tips .

test:
    override:
        - docker run -rm tips dnx -p test/TipsWeb.test/project.json test
